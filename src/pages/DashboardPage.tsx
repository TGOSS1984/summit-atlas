// just route renders for now
export function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p style={{ color: 'var(--text-secondary)' }}>stats and collection rings go here.</p>
    </div>
  )
}